{
  "buildCommand": "npm run build -w client && cp -r client/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://cuttracker-ozsg.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
